@model IEnumerable<DESCUENTOS_ASOCIADOS.Models.TBL_EQUIVALENCIAS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout3.cshtml";
}

<h2>EQUIVALENCIAS</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CODIGO_CONTABLE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TOTAL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CODIGO_CONTABLE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TOTAL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_EQUIVALENCIAS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_EQUIVALENCIAS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_EQUIVALENCIAS })
        </td>
    </tr>
}

</table>
